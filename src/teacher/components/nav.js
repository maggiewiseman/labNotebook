//this will return a component that renders a nav bar
//the nav will have the logo, link to home(dashboard), button to create new assignment, logout, help button, account settingss

export default function Navbar(props) {
    return (
        <nav>

        </nav>
    );
}
